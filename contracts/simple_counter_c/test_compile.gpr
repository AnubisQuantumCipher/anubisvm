project Test_Compile is

   SDK_Root := "../../core/src/sdk";
   VM_Root := "../../core/src/vm";

   for Source_Dirs use (
      "src",
      SDK_Root,
      VM_Root
   );
   for Object_Dir use "obj_test";

   package Compiler is
      for Default_Switches ("Ada") use
         ("-gnatwa", "-gnat2022", "-gnata", "-O0", "-c");
   end Compiler;

end Test_Compile;
