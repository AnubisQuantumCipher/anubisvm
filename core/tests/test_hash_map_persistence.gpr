with "../aegisvm.gpr";

project Test_Hash_Map_Persistence is

   for Languages use ("Ada");
   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Exec_Dir use "../bin";
   for Main use ("test_hash_map_persistence.adb");

   package Compiler is
      for Default_Switches ("Ada") use Aegisvm.Compiler_Switches;
   end Compiler;

   package Binder is
      for Default_Switches ("Ada") use ("-Es");
   end Binder;

   package Linker is
      for Default_Switches ("Ada") use ("-L../lib");
   end Linker;

end Test_Hash_Map_Persistence;
