with "../anubisvm.gpr";

project Test_Address is

   for Languages use ("Ada");
   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";
   for Main use ("test_address.adb");
   for Create_Missing_Dirs use "True";

   package Compiler renames AnubisVM.Compiler;
   package Binder renames AnubisVM.Binder;
   package Builder renames AnubisVM.Builder;

   --  Override linker to avoid duplicate RPATH issues on macOS
   package Linker is
      for Default_Switches ("Ada") use ();
   end Linker;

end Test_Address;
