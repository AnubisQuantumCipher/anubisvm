project Test_MLDSA_Minimal is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";
   for Main use ("test_mldsa_minimal.adb");

   package Compiler is
      for Switches ("Ada") use ("-g", "-gnat2012", "-gnatwa", "-gnata");
   end Compiler;

end Test_MLDSA_Minimal;
